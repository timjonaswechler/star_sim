[package]
name = "bevy_viewer"
version = "0.1.0"
edition = "2024"

[features]
default = []
agent-control = ["dep:agent_control"]

[dependencies]
bevy = "0.19.0"
simulation_core = { path = "../../crates/simulation_core" }
agent_control = { path = "../../crates/agent_control", optional = true, features = ["render"] }
