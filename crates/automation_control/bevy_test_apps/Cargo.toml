[package]
name = "bevy_test_apps"
version = "0.1.0"
edition = "2024"
publish = false
autobins = false

[dependencies]
bevy = { version = "0.19.1", features = [] }
automation_control = { path = "../", optional = true }

[features]
default = []
automation = ["dep:automation_control"]

[[bin]]
name = "context_menu"
path = "src/bin/context_menu.rs"
